Dev:
```
$ npm install
$ npm run dev

# you can now access http://localhost:3000/koth
# going to http://localhost:3000/auth/login fallbacks to dev.tamudatathon.com (except for homepage for some weird reason)
# therefore auth with gatekeeper should be easy peasy
```

This has some starter code that is totally not necessary